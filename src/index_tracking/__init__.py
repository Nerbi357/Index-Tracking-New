"""index_tracking: sparse index tracking of the S&P 500 (weekly, walk-forward).

Reusable engine for the project. Notebooks import from here; every project-defined
function is prefixed ``custom_`` (classes: ``Custom``) so notebook readers can tell our
code from third-party calls at a glance.
"""

__version__ = "0.1.0"
